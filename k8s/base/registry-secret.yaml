apiVersion: v1
kind: Secret
metadata:
  name: ghcr-secret
  namespace: smu-system
type: kubernetes.io/dockerconfigjson
data:
  # This is a placeholder. In a real setup, you'd create this secret directly on the cluster
  # using the kubectl create secret docker-registry command with your GHCR_PAT
  .dockerconfigjson: e30K # Empty JSON object in base64
